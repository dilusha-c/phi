const FOLLOW_UP_VISIT_TYPES = ["Home Visit", "Phone Call", "Hospital Visit"];
const FOLLOW_UP_STATUS = ["Pending", "Completed", "Cancelled"];
const DEFAULT_PAGE = 1;
const DEFAULT_LIMIT = 10;
const MAX_LIMIT = 100;

module.exports = {
  FOLLOW_UP_VISIT_TYPES,
  FOLLOW_UP_STATUS,
  DEFAULT_PAGE,
  DEFAULT_LIMIT,
  MAX_LIMIT,
};
